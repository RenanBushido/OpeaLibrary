## 1. Domain events infrastructure (Domain layer)

- [x] 1.1 Added `IDomainEvent` marker interface at `src/Domain/Events/IDomainEvent.cs` (no external dependencies).
- [x] 1.2 Added `Entity.DomainEvents` (`IReadOnlyCollection<IDomainEvent>`), `protected AddDomainEvent(IDomainEvent)`, and `public ClearDomainEvents()` to `src/Domain/Entities/Entity.cs`.
- [x] 1.3 Added `BookCreatedEvent`/`BookQuantityChangedEvent` records under `src/Domain/Events/`.
- [x] 1.4 Added `LoanCreatedEvent`/`LoanReturnedEvent` records under `src/Domain/Events/`.
- [x] 1.5 `Book.Create` raises `BookCreatedEvent`; `DecreaseQuantity`/`IncreaseQuantity` each raise `BookQuantityChangedEvent` only after the guard clause passes and the mutation happens (guard-throw paths raise nothing).
- [x] 1.6 `Loan.Create` raises `LoanCreatedEvent`; `MarkAsReturned` raises `LoanReturnedEvent` only after the guard clause passes (already-returned throw path raises nothing). `Loan.Restore` (rehydration from storage, not creation) intentionally left untouched — raising `LoanCreatedEvent` there would be wrong. `dotnet build src/Domain/OpeaLibrary.Domain.csproj` succeeds with 0 errors.

## 2. Domain layer tests

- [x] 2.1 Extended the existing `EntityTests.cs`'s `TestEntity` (adding a `RaiseDomainEvent` pass-through to the protected `AddDomainEvent`) with tests for empty-by-default, `AddDomainEvent` appending, and `ClearDomainEvents` clearing.
- [x] 2.2 Added scenarios to `BookTests.cs`/`LoanTests.cs`: `Create` raises the expected event with correct field values; `DecreaseQuantity`/`IncreaseQuantity`/`MarkAsReturned` raise their event only on the success path (`ClearDomainEvents()` called after `Create` in these tests to isolate the mutation's own event from creation's); the guard-throw paths (`DecreaseQuantity` at 0, `MarkAsReturned` when already returned) raise nothing.
- [x] 2.3 Ran `dotnet test ... --collect:"XPlat Code Coverage"`: 100% line/branch coverage confirmed for `src/Domain/Entities/*.cs` and `src/Domain/Events/*.cs` (no gaps found). Test count grew from 42 to 44 after later adding `Book.Restore` (task 4.1's discovery, see `design.md` decision 4a) plus its two tests.

## 3. EF Core interceptor and event dispatch (Infrastructure layer)

- [x] 3.1 Added `DomainEventNotification<TEvent>` at `src/Infrastructure/Persistence/Postgres/Interceptors/DomainEventNotification.cs`. Also added `MediatR` as a new `PackageReference` to `OpeaLibrary.Infrastructure.csproj` (previously only referenced by Application) — needed for `INotification`/`IMediator` in Infrastructure.
- [x] 3.2 Added `DomainEventsInterceptor : SaveChangesInterceptor` at `.../Interceptors/DomainEventsInterceptor.cs`, taking `IMediator` via constructor injection. `SavedChangesAsync` enumerates `ChangeTracker.Entries<Entity>()`, collects+clears each entity's `DomainEvents`, and dispatches each via the reflection-based `Activator.CreateInstance(typeof(DomainEventNotification<>).MakeGenericType(...))` approach (kept simplest per `design.md`, avoids a switch statement needing updates per new event type).
- [x] 3.3 Wired the interceptor into `AddInfraPostgres`: registered `DomainEventsInterceptor` as scoped, switched `AddDbContext` to the `(serviceProvider, options) => ...` overload with `.AddInterceptors(serviceProvider.GetRequiredService<DomainEventsInterceptor>())`. `dotnet build src/Infrastructure/OpeaLibrary.Infrastructure.csproj` and `src/CrossCutting/OpeaLibrary.CrossCutting.csproj` both succeed with 0 errors.

## 4. MongoDB projection notification handlers (Infrastructure layer)

- [x] 4.1 Added `BookCreatedEventHandler.cs`, upserting via `Book.Restore(...)` (new factory, see design.md 4a) + `ReplaceOneAsync(filter, book, IsUpsert: true)`.
- [x] 4.2 Added `BookQuantityChangedEventHandler.cs`: `UpdateOneAsync` with `Builders<Book>.Update.Set(b => b.QuantityAvailable, ...)`.
- [x] 4.3 Added `LoanCreatedEventHandler.cs`, upserting via the existing `Loan.Restore(...)` + `ReplaceOneAsync`.
- [x] 4.4 Added `LoanReturnedEventHandler.cs`: `UpdateOneAsync` with combined `Set(ReturnDate, ...)`/`Set(Status, Returned)`.
- [x] 4.5 All four handlers wrap their Mongo write in try/catch, log via `ILogger<T>.LogError`, and return normally (never rethrow). `dotnet build src/Infrastructure/OpeaLibrary.Infrastructure.csproj` succeeds with 0 errors (required adding `OpeaLibrary.Domain.Enums` to Infrastructure's `GlobalUsings.cs` for `StatusLoan`).

## 5. Wire MediatR to discover the new Infrastructure handlers

- [x] 5.1 Updated `AddApiMediatR` to also `AppDomain.CurrentDomain.Load("OpeaLibrary.Infrastructure")` + `cfg.RegisterServicesFromAssembly(...)` (mirroring the existing `AppDomain.CurrentDomain.Load("OpeaLibrary.Application")` idiom already used there), so the four new `INotificationHandler`s are actually registered.
- [x] 5.2 Added `AddApiMediatR_RegistersInfrastructureNotificationHandlers` to `ApplicationExtensionsTests.cs`. **Deviation from the task's original plan, and from the spec scenario as originally drafted**: calling `AddInfraMongo()` in this test to fully resolve instances would have hit a real bug — `AddInfraMongo` calls `BsonSerializer.RegisterSerializer` unconditionally (no guard), and `InfrastructureExtensionsTests.cs` already calls `AddInfraMongo` once in the same test process; a second call throws `BsonSerializationException` (confirmed empirically). Verifying at the `ServiceDescriptor` level instead (registration presence, not full resolution) — which is exactly what the risk being guarded against (MediatR silently finding zero handlers) actually requires. Updated `specs/crosscutting-unit-tests/spec.md`'s scenario text to match. `dotnet test`: 8/8 pass.

## 6. Infrastructure layer tests

- [x] 6.1 Added `Interceptors/DomainEventsInterceptorTests.cs` using `TestDbContextFactory.Create(interceptor)` (extended to accept `params IInterceptor[]`, backward-compatible default-none) + `Mock<IMediator>`: successful save asserts `Publish` called once with the correctly-typed `DomainEventNotification<BookCreatedEvent>` and that `book.DomainEvents` is cleared afterward; a failed save (FK violation — a `Loan.Create`d for a non-existent `BookId`, same trick used to test the FK constraint in the prior change) asserts `Publish` is never called. Added `Moq`/`MediatR` package references to `Infrastructure.Tests`.
- [x] 6.2 Added one test file per handler (`EventHandlers/BookCreatedEventHandlerTests.cs`, `BookQuantityChangedEventHandlerTests.cs`, `LoanCreatedEventHandlerTests.cs`, `LoanReturnedEventHandlerTests.cs`) using `EphemeralMongoFixture`: each asserts the expected `books`/`loans` document state, and each has a `Handle_WhenMongoWriteFails_DoesNotThrow` test using a `MongoClient` pointed at an unreachable port with a short `ServerSelectionTimeout` (no need to touch the shared ephemeral fixture to simulate failure). **Fixed during this task**: initial `LoanCreatedEventHandlerTests`/`LoanReturnedEventHandlerTests` asserted exact `DateTime` equality against a round-tripped Mongo value — BSON truncates to millisecond precision, so exact equality against `DateTime.UtcNow` (which has sub-millisecond ticks) failed; switched to a `< 5ms` tolerance comparison, matching the existing tolerance pattern already used in `Domain.Tests/LoanTests.cs`.
- [x] 6.3 Ran `dotnet test ... --collect:"XPlat Code Coverage"`: 39/39 pass. Confirmed 100% line/branch coverage on all six new files (`DomainEventNotification.cs`, `DomainEventsInterceptor.cs`, and the four `EventHandlers/*.cs`). The only Postgres/Mongodb-scope gaps in the report are the two already-documented pre-existing exceptions from the prior change (`LoanWriteRepository`'s FK-dead branch, unused `MongoMappings.Configure()`) — unrelated to this change, no new gaps introduced.

## 7. End-to-end verification

- [x] 7.1 Ran fresh (`docker compose down -v` then `up -d`) and started the API. `POST /api/books` → `201`. `GET /api/books` (list) → `200` with the created book's data — **the core gap this whole change exists to fix is confirmed resolved**. `GET /api/books/{id}` still `500`, but now for a *different*, pre-existing, unrelated reason: `BookEndpoint.GetBookById` maps `GetBookByIdResponse -> BookResponse` via AutoMapper, but `MappingProfile` never registers that map (confirmed via `git diff` — `MappingProfile.cs`/`BookEndpoint.cs` untouched by this change). This bug was invisible before because the book was never found in Mongo at all (masked by the `KeyNotFoundException` "not found" path this change just fixed); now that the book *is* found, this separate mapping gap is exposed. Reported to the user; not fixed here (out of scope — Application/Presentation mapping config, not the sync mechanism).
- [x] 7.2 `POST /api/loans/request` → `201`. Verified directly via `psql`/`mongosh`: Postgres `tb_books.QuantityAvailable` and Mongo `books.QuantityAvailable` both dropped to `2`; the new `tb_loans` row and the new Mongo `loans` document match on `Id`/`BookId`/`Status`. Could not exercise the return flow via HTTP: found a second pre-existing, unrelated bug in `LoanEndpoint.cs` (untouched by this change) — `ReturnLoan`'s handler sends a `RequestLoanCommand` instead of `ReturnLoanCommand` (copy-paste bug), and `GetAllLoans` sends a `GetAllBooksRequest` instead of `GetAllLoansRequest`. Reported to the user; not fixed here. The identical pipeline (interceptor → MediatR → handler) is already proven correct for `LoanReturnedEvent` specifically via passing integration tests (`LoanReturnedEventHandlerTests`, `DomainEventsInterceptorTests`) — sufficient verification given the HTTP path is blocked by an unrelated bug, not by anything this change built.
- [x] 7.3 See below — full solution build and all four test projects run clean with no regressions.
