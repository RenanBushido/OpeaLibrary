## 1. Fix Docker Compose MongoDB port mapping

- [x] 1.1 Changed `db_read`'s `ports` mapping from `"8081:8081"` to `"27017:27017"` in `src/docker-compose.yml` (confirmed via Context7/`docker/awesome-compose` reference examples that `27017:27017` is the standard Mongo port mapping).
- [x] 1.2 Ran `docker compose -f src/docker-compose.yml down -v` then `up -d`: both `db_save` and `db_read` started (`docker ps` confirms `0.0.0.0:27017->27017/tcp` and `0.0.0.0:5432->5432/tcp`). Verified with authenticated checks using `src/.env` credentials — `mongosh "mongodb://...@localhost:27017/?authSource=admin"` returned `{ok: 1}`, `psql -U OpeaLibrary -d OpeaLibrary -c "SELECT 1;"` returned a row.

## 2. Split connection strings by environment

- [x] 2.1 `src/Presentation/appsettings.json`: `OpeaLibraryWriteConnection` now `Server=db_save;Port=5432;Database=OpeaLibrary;User Id=OpeaLibrary;Password=OpeaLibrary@123;`; `OpeaLibraryReadConnection` now `mongodb://OpeaLibrary:OpeaLibrary!123@db_read:27017/` — hosts match the Compose service names, credentials match `src/.env`. Confirmed the Npgsql connection-string format (`Server=...;Port=...;Database=...;User Id=...;Password=...;`) against Context7's Npgsql docs.
- [x] 2.2 `src/Presentation/appsettings.Development.json`: `OpeaLibraryWriteConnection` now `Server=localhost;Port=5432;...`; `OpeaLibraryReadConnection` now `mongodb://OpeaLibrary:OpeaLibrary!123@localhost:27017/` — same credentials, `localhost` + the host-published ports from task 1.
- [x] 2.3 Confirmed: `appsettings.Development.json` no longer references host `mongo` anywhere.

## 3. Add Postgres schema provisioning

- [x] 3.1 Ran `dotnet ef migrations add InitialCreate --project src/Infrastructure/OpeaLibrary.Infrastructure.csproj --startup-project src/Presentation/OpeaLibrary.Api.csproj --output-dir Migrations`. Generated `src/Infrastructure/Migrations/20260716160307_InitialCreate.cs` (+ Designer + `OpeaLibraryDbContextModelSnapshot.cs`).
- [x] 3.2 Reviewed `Up()` against `BookConfig`/`LoanConfig` line by line: `tb_books` (Id uuid PK, Title varchar(200) required, Author varchar(100) required, PublishedYear/QuantityAvailable required) and `tb_loans` (Id uuid PK, BookId uuid required, LoanDate required, ReturnDate optional, `FK_tb_loans_tb_books_BookId` with `onDelete: ReferentialAction.Restrict`) match exactly. No config changes needed; no regeneration required.
- [x] 3.3 Added `OpeaLibraryDbContext`/`Microsoft.EntityFrameworkCore` to `src/Presentation/GlobalUsings.cs`; in `Program.cs`'s existing `IsDevelopment()` scope block, resolved `OpeaLibraryDbContext` and called `await dbContext.Database.MigrateAsync()` before the existing `IMongoDatabaseInitializer.EnsureDatabaseCreatedAsync()` call. Pattern matches the Microsoft-documented "Apply Migrations in ASP.NET Core Startup" snippet (confirmed via Context7/EF Core docs), which also confirmed this startup-time approach is appropriate for local development but not production — consistent with `design.md`'s Non-Goals. `dotnet build src/OpeaLibrary.slnx` succeeds with 0 errors.

## 4. End-to-end verification

- [x] 4.1 Ran fresh (`docker compose down -v` then `up -d`, confirmed `tb_books`/`tb_loans` absent via `\dt` beforehand). Started the API (`dotnet run`, `ASPNETCORE_ENVIRONMENT=Development`): logs show `Applying migration '20260716160307_InitialCreate'`, then the Mongo initializer creating `books`/`loans`, then `Now listening on: http://localhost:5139`. No connection errors. Confirmed via `psql \dt` that `tb_books`/`tb_loans` (+ `__EFMigrationsHistory`) now exist.
- [x] 4.2 `POST /api/books` → `201 Created`, row confirmed present in `tb_books` via `psql`. `GET /api/books` → `200 []` (Mongo reachable, no connection error). **Finding surfaced and resolved with the user**: `GET /api/books/{id}` returns `500` because the write is never propagated from Postgres to MongoDB — `BookWriteRepository.AddBookAsync` only writes to Postgres; nothing in `src/Application`/`src/Infrastructure` synchronizes the CQRS read side. Confirmed directly: the Postgres row exists, the Mongo `books` collection has 0 documents. This is a separate, pre-existing architectural gap (no read-model projection/sync mechanism), not a connectivity problem — this change's actual goal (both databases reachable, schema/collections provisioned) is fully verified and working. Per explicit user decision, left out of this change's scope; to be addressed as a follow-up change.
- [x] 4.3 Restarted the API a second time against the now-provisioned databases: startup log shows the migration step completing with no pending migrations to apply and the Mongo initializer logging `Collection 'books' already exists.`/`Collection 'loans' already exists.` — both `Database.MigrateAsync()` and `EnsureDatabaseCreatedAsync()` are idempotent against already-provisioned state, no errors.
