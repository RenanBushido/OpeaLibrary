## 1. Restore `OpeaLibrary.Infrastructure.Tests` compilation (Postgres/SQLite side)

- [x] 1.1 Add `Microsoft.EntityFrameworkCore.Sqlite` `PackageReference` to `tests/OpeaLibrary.Infrastructure.Tests/OpeaLibrary.Infrastructure.Tests.csproj` (pinned `SQLitePCLRaw.bundle_e_sqlite3` to `3.0.3` to remediate a known-vulnerable transitive `2.1.11`).
- [x] 1.2 Rewrote `TestDbContextFactory.Create()` to open a `SqliteConnection("DataSource=:memory:")`, build `OpeaLibraryDbContext` with `UseSqlite(connection)`, call `Database.EnsureCreated()`, and return a new `SqliteTestDatabase : IDisposable` wrapper exposing `.Context` and disposing both the context and the connection.
- [x] 1.3 Updated every `TestDbContextFactory.Create()` call site to the `using var database = ...; var dbContext = database.Context;` pattern — `UnitOfWorkTests.cs`, `BookRepositoryTests.cs`/`LoanRepositoryTests.cs`, plus two call sites not originally listed in this task that also broke on the factory's new return type: `Context/OpeaLibraryDbContextTests.cs` and `Config/BookConfigTests.cs`/`Config/LoanConfigTests.cs`.
- [x] 1.4 Renamed `Repositories/BookRepositoryTests.cs` → `Repositories/BookWriteRepositoryTests.cs` (class `BookRepositoryTests` → `BookWriteRepositoryTests`); replaced `unitOfWork.BookRepository` with `unitOfWork.BookWriteRepository` throughout.
- [x] 1.5 Removed the `GetBookByIdAsync`/`GetAllBooksAsync` read-path tests from `BookWriteRepositoryTests`; `AddBookAsync_PersistsBook_RetrievableAfterSave` now asserts persistence via a direct `dbContext.Books.FindAsync(...)` query.
- [x] 1.6 Renamed `Repositories/LoanRepositoryTests.cs` → `Repositories/LoanWriteRepositoryTests.cs` (class `LoanRepositoryTests` → `LoanWriteRepositoryTests`); replaced `unitOfWork.BookRepository`/`unitOfWork.LoanRepository`/`unitOfWork.BookWriteRepository.GetBookByIdAsync` usages with `unitOfWork.LoanWriteRepository` for writes and direct `dbContext.Books`/`dbContext.Loans` queries for persisted-state assertions. Also dropped `ReturnLoanAsync_WhenAssociatedBookNoLongerExists_StillMarksReturned` — it seeded an orphaned `Loan` (no associated `Book`) directly via the `DbSet`, which SQLite's FK enforcement now correctly rejects with `FOREIGN KEY constraint failed`; that precondition was only ever reachable under the EF InMemory provider's lack of constraint enforcement. See `design.md` decision 5.
- [x] 1.7 Removed the `GetAllLoansAsync_*` tests from `LoanWriteRepositoryTests` (moved to the new `LoanReadRepositoryTests`, see section 2).
- [x] 1.8 Fixed `UnitOfWorkTests.cs`: replaced `unitOfWork.BookRepository`/`unitOfWork.LoanRepository` with `unitOfWork.BookWriteRepository`/`unitOfWork.LoanWriteRepository`, and changed the `CommitAsync_PersistsStagedChanges` assertion to query `dbContext.Books` directly.
- [x] 1.9 Ran `dotnet test tests/OpeaLibrary.Infrastructure.Tests/OpeaLibrary.Infrastructure.Tests.csproj`: 0 compile errors, 19/19 tests pass (after dropping the FK-violating scenario in 1.6, from an initial 19 passed/1 failed).

## 2. Add MongoDB read-repository test coverage

- [x] 2.1 Checked NuGet: `EphemeralMongo6` `2.0.0` targets `.NETStandard2.0` (compatible with `net10.0`), bundles MongoDB 6.0.21 native binaries via per-RID runtime dependency packages (no network fetch at test time), and restored cleanly alongside the existing `MongoDB.Driver 3.10.0`. Pinned `2.0.0`.
- [x] 2.2 Added `EphemeralMongo6` and an explicit `MongoDB.Driver 3.10.0` `PackageReference` to `tests/OpeaLibrary.Infrastructure.Tests/OpeaLibrary.Infrastructure.Tests.csproj`.
- [x] 2.3 Added `Repositories/EphemeralMongoFixture.cs`: `EphemeralMongoFixture : IAsyncLifetime` starts one `MongoRunner` (real API is synchronous `MongoRunner.Run(options)`, not the `RunAsync` initially assumed) per `EphemeralMongoCollection` (one shared instance for the whole `Repositories` test collection, not per class or per test), exposes `IMongoClient`/`CreateIsolatedDatabase()`, and registers the `GuidSerializer` once (guarded, mirroring `InfrastructureExtensions.AddInfraMongo`). `DisposeAsync` swallows a known `FileNotFoundException` from `EphemeralMongo.Core 2.0.0`'s internal shutdown path, which references `MongoDB.Driver 2.28.0`'s `MongoDB.Driver.Core` assembly — an assembly this solution's unified `MongoDB.Driver 3.10.0` no longer ships; see `design.md` decision 2 addendum.
- [x] 2.4 Added `Repositories/BookReadRepositoryTests.cs`: seeds `Book` documents directly into a `books` collection on an isolated per-test database, covers `GetBookByIdAsync` (exists / does not exist) and `GetAllBooksAsync` (empty / single / multiple).
- [x] 2.5 Added `Repositories/LoanReadRepositoryTests.cs`: seeds `Loan` documents directly into a `loans` collection, covers `GetAllLoansAsync` (empty / single / multiple).
- [x] 2.6 Each test method (not just each class) calls `fixture.CreateIsolatedDatabase()`, which returns a uniquely-named database per call against the one shared ephemeral instance — stronger isolation than the per-class minimum in the `Ephemeral MongoDB Test Infrastructure` requirement.
- [x] 2.7 Ran `dotnet test tests/OpeaLibrary.Infrastructure.Tests/OpeaLibrary.Infrastructure.Tests.csproj` three times: 27/27 pass every run (19 SQLite write-repository/config/context tests + 8 new Mongo read-repository tests), exit code 0, no warnings.

## 3. Add `OpeaLibrary.CrossCutting.Tests`

- [x] 3.1 Created `tests/OpeaLibrary.CrossCutting.Tests/OpeaLibrary.CrossCutting.Tests.csproj` mirroring the other test projects' shape, referencing `src/CrossCutting/OpeaLibrary.CrossCutting.csproj`. Did **not** add it to `src/OpeaLibrary.slnx` — inspection showed none of the three existing test projects (`Domain.Tests`, `Application.Tests`, `Infrastructure.Tests`) are referenced there either (the slnx only lists `src/` projects; tests are run standalone via `dotnet test <csproj>`), so adding only the new one would be inconsistent with the repo's actual, existing convention rather than following it.
- [x] 3.2 Added `GlobalUsings.cs` for the new project (MediatR, FluentValidation, AutoMapper, `Microsoft.Extensions.DependencyInjection`, `Microsoft.Extensions.Logging`, `Microsoft.Extensions.Configuration`, `OpeaLibrary.CrossCutting.Extensions`, relevant Application/Domain/Infrastructure namespaces).
- [x] 3.3 Added `Extensions/ApplicationExtensionsTests.cs` covering `AddApiMediatR`, `AddApiAutoMapper`, `AddApiValidators`. Both `AddApiMediatR`'s and `AddApiAutoMapper`'s resolution paths require `ILoggerFactory` (MediatR's license-check path, AutoMapper's internal logger) — a dependency not obvious from `ApplicationExtensions`' own signature — so each test also calls `services.AddLogging()` first; added `Microsoft.Extensions.Logging` package accordingly.
- [x] 3.4 Added `Extensions/InfrastructureExtensionsTests.cs` covering `AddInfraPostgres`/`AddInfraMongo`, asserting `ServiceDescriptor` registrations directly on the `IServiceCollection` (no `BuildServiceProvider()`/resolve) so no live Postgres/Mongo connection is ever attempted; a placeholder `IConfiguration` is built via `ConfigurationBuilder().AddInMemoryCollection(...)`, which as of `Microsoft.Extensions.Configuration 10.0.10` ships directly in the core package — the previously-assumed separate `Microsoft.Extensions.Configuration.InMemory` package no longer resolves on NuGet for this version line (404, no versions listed) and isn't needed.
- [x] 3.5 Ran `dotnet test tests/OpeaLibrary.CrossCutting.Tests/OpeaLibrary.CrossCutting.Tests.csproj`: 7/7 pass, exit code 0, no external Postgres/MongoDB connection required.

## 4. Align `OpeaLibrary.Application.Tests` naming

- [x] 4.1 Renamed `Books/Queries/GetAllBookQueryHandlerTests.cs` → `Books/Queries/GetAllBooksQueryHandlerTests.cs` (class `GetAllBookQueryHandlerTests` → `GetAllBooksQueryHandlerTests`); content already targeted `GetAllBooksQueryHandler`/`GetAllBooksRequest`/`GetAllBooksResponse`/`IBookReadRepository` correctly, confirming this was rename-only as expected.
- [x] 4.2 Ran `dotnet test tests/OpeaLibrary.Application.Tests/OpeaLibrary.Application.Tests.csproj`: 33/33 pass, 0 errors.

## 5. Full-solution verification

- [x] 5.1 Ran `dotnet build src/OpeaLibrary.slnx`: 0 errors (one transient `Internal CLR error` on the first attempt, unrelated to this change — a clean retry built successfully).
- [x] 5.2 Ran `dotnet test` across all four projects: `Domain.Tests` 32/32, `Application.Tests` 33/33, `Infrastructure.Tests` 29/29 (27 → 29 after task 5.3's `MongoDatabaseInitializerTests` addition), `CrossCutting.Tests` 7/7 — 101/101 total, all pass, no external database/service dependency.
- [x] 5.3 Ran `dotnet test --collect:"XPlat Code Coverage"` for all three projects. `Application.Tests`: 100% of `src/Application/`, no gaps. `Infrastructure.Tests`: 100% of `src/Infrastructure/Persistence/Postgres/` and `.../Mongodb/` except the two documented exceptions found and resolved with the user during this task — added `Extensions/MongoDatabaseInitializerTests.cs` to close a real, previously-missed gap (`MongoDatabaseInitializer` was 0% covered, not accounted for in the original task breakdown), then confirmed the remaining two gaps (`LoanWriteRepository`'s FK-dead null-conditional branch; unused `MongoMappings.Configure()`) are legitimate exceptions, now documented in `specs/infrastructure-unit-tests/spec.md`. `CrossCutting.Tests`: 100% of `src/CrossCutting/` except one documented exception (the `IDbConnection`/`IMongoClient` factory delegate bodies in `AddInfraPostgres`/`AddInfraMongo`, never resolved by design to avoid a live-connection dependency), documented in `specs/crosscutting-unit-tests/spec.md`.
- [x] 5.4 Re-read `openspec/specs/application-unit-tests/spec.md`, `openspec/specs/infrastructure-unit-tests/spec.md`, and `specs/crosscutting-unit-tests/spec.md` deltas against the final test code: every requirement/scenario has a corresponding test, with the three documented coverage exceptions above being the only deviations, all traced to `design.md` decisions rather than gaps in test design.
